# FastVideo/FastWan Requirements
# NOTE: PyTorch is installed separately via pip with --index-url for cu128 wheels
# NOTE: numpy<2.0 is CRITICAL - FastVideo breaks with numpy 2.x

numpy<2.0
diffusers
transformers
accelerate
safetensors
einops
sentencepiece
huggingface_hub
imageio
imageio-ffmpeg
opencv-python
ftfy
